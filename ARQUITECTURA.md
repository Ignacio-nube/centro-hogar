# 🏗️ Arquitectura del Sistema - Backend

## 📊 Visión General

```
┌─────────────────────────────────────────────────────────────┐
│                        FRONTEND                              │
│              (React, Angular, Vue, etc.)                     │
└──────────────────────┬──────────────────────────────────────┘
                       │ HTTP/REST API
                       │ Authorization: Bearer JWT
                       ▼
┌─────────────────────────────────────────────────────────────┐
│                    BACKEND (Express + TypeScript)            │
│                                                              │
│  ┌────────────┐   ┌──────────────┐   ┌──────────────┐     │
│  │   Routes   │──▶│ Controllers  │──▶│   Services   │     │
│  └────────────┘   └──────────────┘   └──────┬───────┘     │
│                                               │              │
│  ┌────────────┐   ┌──────────────┐          │              │
│  │ Middleware │   │   Models     │          │              │
│  │   (Auth)   │   │ (Interfaces) │          │              │
│  └────────────┘   └──────────────┘          │              │
└─────────────────────────────────────────────┼──────────────┘
                                               │
                                               ▼
┌─────────────────────────────────────────────────────────────┐
│                   BASE DE DATOS MySQL                        │
│                     (datos-negocio)                          │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐      │
│  │ CLIENTE  │ │ PRODUCTO │ │  VENTA   │ │PROVEEDOR │      │
│  └──────────┘ └──────────┘ └──────────┘ └──────────┘      │
└─────────────────────────────────────────────────────────────┘
```

## 🔄 Flujo de una Petición

```
1. Cliente hace petición HTTP
   ↓
2. Express recibe la petición
   ↓
3. Middleware de CORS/Helmet (seguridad)
   ↓
4. Middleware de Auth (verifica JWT)
   ↓
5. Middleware de Roles (verifica permisos)
   ↓
6. Route dirige al Controller
   ↓
7. Controller procesa y llama al Service
   ↓
8. Service ejecuta lógica de negocio
   ↓
9. Service consulta/modifica la Base de Datos
   ↓
10. Respuesta regresa por el mismo camino
   ↓
11. Middleware de Errores (si hay errores)
   ↓
12. Respuesta JSON al cliente
```

## 📂 Estructura de Capas

### Capa de Presentación (Routes)
```typescript
// Responsabilidad: Definir endpoints y aplicar middlewares

GET    /api/productos          → ProductoController.obtenerTodos()
POST   /api/ventas             → VentaController.crear()
GET    /api/reportes/ventas    → ReporteController.reporteVentas()
```

### Capa de Control (Controllers)
```typescript
// Responsabilidad: Recibir request, validar básico, llamar service

async obtenerPorId(req, res) {
  try {
    const producto = await productoService.obtenerPorId(req.params.id);
    res.json(producto);
  } catch (error) {
    res.status(error.statusCode).json({ error: error.message });
  }
}
```

### Capa de Negocio (Services)
```typescript
// Responsabilidad: Lógica de negocio compleja

async crear(datosVenta) {
  // 1. Validar cliente
  // 2. Verificar stock
  // 3. Crear transacción
  // 4. Descontar stock
  // 5. Generar cronograma
  // 6. Retornar resultado
}
```

### Capa de Datos (Database)
```typescript
// Responsabilidad: Conexión y queries a MySQL

await pool.query('SELECT * FROM PRODUCTOS WHERE id_producto = ?', [id]);
```

## 🔐 Flujo de Autenticación

```
┌─────────────┐
│   Cliente   │
└──────┬──────┘
       │ 1. POST /api/auth/login
       │    { usuario, contraseña }
       ▼
┌─────────────────────────────┐
│   AuthController.login()    │
└──────┬──────────────────────┘
       │ 2. Buscar usuario en BD
       ▼
┌─────────────────────────────┐
│   AuthService.login()       │
│   - Verificar contraseña    │
│   - Generar JWT             │
└──────┬──────────────────────┘
       │ 3. Retornar token
       ▼
┌─────────────┐
│   Cliente   │
│ Guarda JWT  │
└─────────────┘

Siguientes peticiones:
Authorization: Bearer eyJhbGciOiJIUzI1NiIs...
```

## 💰 Flujo de Venta a Crédito

```
┌────────────────────────────────────────────────────────────┐
│ 1. POST /api/ventas                                        │
│    - id_cliente, productos, plan_pagos                     │
└────────────────┬───────────────────────────────────────────┘
                 │
                 ▼
┌────────────────────────────────────────────────────────────┐
│ 2. VentaService.crear()                                    │
│    ┌──────────────────────────────────────────────┐       │
│    │ a. Validar cliente (DNI, estado)              │       │
│    │ b. Verificar que no tenga mora               │       │
│    │ c. Validar stock de productos                │       │
│    └──────────────────────────────────────────────┘       │
└────────────────┬───────────────────────────────────────────┘
                 │
                 ▼
┌────────────────────────────────────────────────────────────┐
│ 3. Transacción en Base de Datos                            │
│    ┌──────────────────────────────────────────────┐       │
│    │ a. INSERT INTO PAGO                           │       │
│    │ b. INSERT INTO VENTA                          │       │
│    │ c. INSERT INTO DETALLE_VENTA (por producto)   │       │
│    │ d. UPDATE PRODUCTOS SET stock = stock - X     │       │
│    │ e. INSERT INTO DETALLE_PAGO (cronograma)      │       │
│    │ f. COMMIT                                     │       │
│    └──────────────────────────────────────────────┘       │
└────────────────┬───────────────────────────────────────────┘
                 │
                 ▼
┌────────────────────────────────────────────────────────────┐
│ 4. Respuesta al Cliente                                    │
│    {                                                       │
│      mensaje: "Venta registrada",                          │
│      id_venta: 123                                         │
│    }                                                       │
└────────────────────────────────────────────────────────────┘
```

## 💳 Flujo de Registro de Pago

```
Cliente con deuda → Registrar Pago → Actualizar Estado

┌─────────────────────┐
│ Venta ID: 1         │
│ Total: $10,000      │
│ Pagado: $4,000      │
│ Estado: Pendiente   │
└──────────┬──────────┘
           │
           │ POST /api/cobranzas/registrar-pago
           │ { id_venta: 1, monto: 3000 }
           ▼
┌──────────────────────────────┐
│ CobranzaService              │
│ 1. Validar venta existe      │
│ 2. Calcular saldo            │
│ 3. Validar monto <= saldo    │
│ 4. UPDATE PAGO               │
│ 5. INSERT DETALLE_PAGO       │
│ 6. Si pagado completo:       │
│    UPDATE VENTA estado       │
└──────────┬───────────────────┘
           │
           ▼
┌─────────────────────┐
│ Venta ID: 1         │
│ Total: $10,000      │
│ Pagado: $7,000      │  ← Actualizado
│ Estado: Parcial     │  ← Actualizado
└─────────────────────┘
```

## 📦 Flujo de Gestión de Stock

```
╔═══════════════════════════════════════════════════════════╗
║                    ENTRADA DE STOCK                       ║
╚═══════════════════════════════════════════════════════════╝

Proveedor → Registrar Compra → Stock Aumenta

POST /api/proveedores/compra
{
  id_proveedor: 1,
  detalles: [
    { id_producto: 1, cantidad: 50, precio_unit: 3000 }
  ]
}
              ↓
┌──────────────────────────────┐
│ Stock Antes:  10             │
└──────────────────────────────┘
              ↓
  UPDATE PRODUCTOS SET stock = stock + 50
              ↓
┌──────────────────────────────┐
│ Stock Después: 60            │
└──────────────────────────────┘


╔═══════════════════════════════════════════════════════════╗
║                    SALIDA DE STOCK                        ║
╚═══════════════════════════════════════════════════════════╝

Venta → Descontar Stock Automático

POST /api/ventas
{ productos: [{ id: 1, cantidad: 3 }] }
              ↓
┌──────────────────────────────┐
│ Stock Antes:  60             │
└──────────────────────────────┘
              ↓
  UPDATE PRODUCTOS SET stock = stock - 3
              ↓
┌──────────────────────────────┐
│ Stock Después: 57            │
└──────────────────────────────┘


╔═══════════════════════════════════════════════════════════╗
║                 DEVOLUCIÓN (Cancelación)                  ║
╚═══════════════════════════════════════════════════════════╝

POST /api/ventas/123/cancelar
              ↓
  1. Registrar devolución
  2. UPDATE PRODUCTOS SET stock = stock + 3
  3. UPDATE VENTA SET estado = 'cancelada'
```

## 📊 Módulos del Sistema

```
┌─────────────────────────────────────────────────────────────┐
│                        AUTENTICACIÓN                         │
│  • Login / Logout                                           │
│  • JWT Tokens                                               │
│  • Roles: Admin, Vendedor, Enc. Stock                      │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│                         CLIENTES                            │
│  • CRUD de clientes                                         │
│  • Validación DNI único                                     │
│  • Bloqueo por mora automático                              │
│  • Historial de compras                                     │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│                        PRODUCTOS                             │
│  • CRUD de productos                                        │
│  • Categorías                                               │
│  • Control automático de stock                              │
│  • Alertas de stock bajo                                    │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│                          VENTAS                             │
│  • Ventas al contado / crédito                              │
│  • Descuento automático de stock                            │
│  • Generación de cronograma de pagos                        │
│  • Cancelación con devolución                               │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│                        COBRANZAS                             │
│  • Registro de pagos                                        │
│  • Pagos parciales                                          │
│  • Cuentas por cobrar                                       │
│  • Control de mora                                          │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│                       PROVEEDORES                            │
│  • CRUD de proveedores                                      │
│  • Registro de compras                                      │
│  • Entrada automática de stock                              │
│  • Cuentas por pagar                                        │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│                        REPORTES                              │
│  • Ventas (día, semana, mes, año)                           │
│  • Cobranzas                                                │
│  • Flujo de efectivo                                        │
│  • Top productos / clientes                                 │
│  • Inventario                                               │
└─────────────────────────────────────────────────────────────┘
```

## 🔒 Matriz de Permisos

```
┌────────────────────┬──────────┬──────────┬──────────────┐
│ Acción             │  Admin   │ Vendedor │ Enc. Stock   │
├────────────────────┼──────────┼──────────┼──────────────┤
│ Gestionar Usuarios │    ✅    │    ❌    │      ❌      │
│ Ver Clientes       │    ✅    │    ✅    │      ✅      │
│ Crear Clientes     │    ✅    │    ✅    │      ❌      │
│ Bloquear Clientes  │    ✅    │    ❌    │      ❌      │
│ Ver Productos      │    ✅    │    ✅    │      ✅      │
│ Crear Productos    │    ✅    │    ❌    │      ✅      │
│ Eliminar Productos │    ✅    │    ❌    │      ❌      │
│ Registrar Ventas   │    ✅    │    ✅    │      ❌      │
│ Cancelar Ventas    │    ✅    │    ❌    │      ❌      │
│ Registrar Pagos    │    ✅    │    ✅    │      ❌      │
│ Ver Proveedores    │    ✅    │    ✅    │      ✅      │
│ Crear Proveedores  │    ✅    │    ❌    │      ✅      │
│ Registrar Compras  │    ✅    │    ❌    │      ✅      │
│ Reportes Ventas    │    ✅    │    ✅    │      ❌      │
│ Reporte Flujo $    │    ✅    │    ❌    │      ❌      │
│ Reportes Stock     │    ✅    │    ❌    │      ✅      │
└────────────────────┴──────────┴──────────┴──────────────┘
```

## 🗄️ Modelo de Datos Simplificado

```
CLIENTE
├── id_cliente (PK)
├── DNI_cliente (UNIQUE)
├── estado_cliente (activo/bloqueado)
└── [datos personales]

PRODUCTO
├── id_producto (PK)
├── stock ← Se actualiza automáticamente
├── stock_minimo
├── categoria
└── id_proveedor (FK)

VENTA
├── id_venta (PK)
├── id_cliente (FK) ─────┐
├── id_usuario (FK)      │
├── id_pago (FK) ────┐   │
├── total_venta      │   │
└── estado_venta     │   │
                     │   │
PAGO                 │   │
├── id_pago (PK) ◄───┘   │
├── monto                │
├── estado               │
└── id_tipo_pago (FK)    │
                         │
DETALLE_VENTA            │
├── id_venta (FK)        │
├── id_producto (FK) ────┼─► Descuenta stock
├── cantidad             │
└── precio_unitario      │
                         │
CLIENTE ◄────────────────┘
Puede ser bloqueado si tiene mora
```

## ⚡ Operaciones Automáticas

```
┌─────────────────────────────────────────────────────────┐
│                 AUTOMATIZACIONES                         │
└─────────────────────────────────────────────────────────┘

📦 AL REGISTRAR VENTA:
   → Descuenta stock automáticamente
   → Genera cronograma de pagos (si es crédito)
   → Valida estado del cliente

💳 AL REGISTRAR PAGO:
   → Actualiza saldo pendiente
   → Cambia estado de venta si está completo
   → Registra en historial

🚫 AL CANCELAR VENTA:
   → Devuelve stock
   → Registra devolución
   → Cambia estado a cancelada

🏭 AL REGISTRAR COMPRA:
   → Aumenta stock automáticamente
   → Registra en cuentas por pagar

⏰ CADA 24 HORAS:
   → Backup automático de BD
   → (opcional) Revisar cuentas vencidas
```

## 🔧 Tecnologías y Librerías

```
Framework Web:
├── express          → Servidor HTTP
└── cors, helmet     → Seguridad

Lenguaje:
├── typescript       → Tipado estático
└── ts-node          → Ejecución en desarrollo

Base de Datos:
├── mysql2           → Driver MySQL
└── promises         → Soporte async/await

Autenticación:
├── jsonwebtoken     → JWT tokens
└── bcryptjs         → Hash de contraseñas

Desarrollo:
├── nodemon          → Hot reload
└── dotenv           → Variables de entorno
```

---

📚 **Referencia Rápida**
- [README.md](README.md) - Documentación completa
- [INSTALACION-WINDOWS.md](INSTALACION-WINDOWS.md) - Guía de instalación
- [INICIO-RAPIDO.md](INICIO-RAPIDO.md) - Inicio rápido
